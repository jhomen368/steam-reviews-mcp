#!/usr/bin/env node

/**
 * Steam Reviews MCP Server
 * Provides AI agents access to Steam game review data and analysis
 */

async function main() {
  console.log('Steam Reviews MCP Server - Initializing...');
  // Server implementation will be added in later stages
}

main().catch((error) => {
  console.error('Fatal error:', error);
  process.exit(1);
});
